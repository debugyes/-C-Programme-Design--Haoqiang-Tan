
/*#include<stdio.h>
int main(void)
{
    int n, i;
    printf("please enter a integer number, n=?");
    scanf("%d", &n);
    for (i = 2; i < n; i++)
    {
        if (n%i == 0)
        {
            break;
        }
    }

    if (i < n)
    {
        printf("%d is not a prime number.", n);
    }
    else
    {
        printf("%d is a prime number.", n);
    }
    
    
    
    return 0;
}*/

#include<stdio.h>
#include<math.h>

int main(void)
{
    int n, i, k;
    printf("请输入一个素数:");
    scanf("%d", &n);
    return 0;
}